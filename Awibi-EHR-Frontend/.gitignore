# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets. Vite inlines anything prefixed VITE_ into the built bundle, so an
# .env here becomes public the moment the site is deployed — it must never be
# committed as well.
.env
.env.*
!.env.example

# Keys and database exports
*.pem
*.key
*.backup
*.backup.gz
*.dump
